import styled from 'styled-components';

import { Button } from '@mui/material';

const LikeButton = styled(Button)`
  && img {
    height: 1.8em;
    padding: 0 0.5em;
  }
`;

export { LikeButton };
